const nombre = "heroe1"
let poder = 70
let energía = 80
const ciudad = "ciudad1"
const enemigo = "enemigo1"

console.log(`el ${nombre} tiene ${poder} de poder, tambien tiene ${energía} de energia, vive en ${ciudad} y su enemig es ${enemigo}`)
console.log(`pelearon y ahora le queda ${energía-50}`)

// "Crea un simulador de superhéroe usando SOLO variables y console.log. Debe tener: nombre, poder, energía, ciudad y enemigo. Luego modifica la energía y muestra el cambio."